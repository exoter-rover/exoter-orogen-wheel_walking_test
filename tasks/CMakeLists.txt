# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(wheel_walking_testTaskLib)
ADD_LIBRARY(${WHEEL_WALKING_TEST_TASKLIB_NAME} SHARED 
    ${WHEEL_WALKING_TEST_TASKLIB_SOURCES})
add_dependencies(${WHEEL_WALKING_TEST_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${WHEEL_WALKING_TEST_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${WHEEL_WALKING_TEST_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${WHEEL_WALKING_TEST_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${WHEEL_WALKING_TEST_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${WHEEL_WALKING_TEST_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${WHEEL_WALKING_TEST_TASKLIB_HEADERS}
    DESTINATION include/orocos/wheel_walking_test)

